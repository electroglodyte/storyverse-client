// Empty placeholder for scene extraction - to be implemented in the future
import { v4 as uuidv4 } from 'uuid';

// Basic scene detection - this will be enhanced in the future
export const extractScenes = (text: string, storyId: string): any[] => {
  // For now, we'll return an empty array
  // Scene detection requires more sophisticated analysis than the other entity types
  // In a future update, we'll implement proper scene detection
  return [];
};
